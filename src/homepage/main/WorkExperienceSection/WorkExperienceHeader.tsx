import styled from "styled-components";
import { colors } from "../../../styles";

export const WorkExperienceHeader = styled.h3`
    color: ${colors.secondaryColor};
    font-variant: small-caps;
    font-size: 1.6em;

    margin-top: 0.5em;
    margin-bottom: 0.5em;
`;
