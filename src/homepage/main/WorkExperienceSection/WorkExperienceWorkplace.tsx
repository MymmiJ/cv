import styled from "styled-components";
import { colors } from "../../../styles";

export const WorkExperienceWorkplace = styled.strong`
    padding-right: 0.5em;
    border-right: solid 2px ${colors.primaryColor};
    margin-right: 0.5em;
`;
