const primaryColor = '#213547';
const secondaryColor = '#bf5249';
const tertiaryColor = '#00003f';

const colors = {
    primaryColor,
    secondaryColor,
    tertiaryColor
};

const mobileBreakpoint = '720px';

const breakpoints = {
    mobileBreakpoint,
};

export {
    colors,
    breakpoints
};